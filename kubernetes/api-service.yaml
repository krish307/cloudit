apiVersion: v1
kind: Service
metadata:
  name: cloudit-api
  namespace: cloudit
  labels:
    app: cloudit-api
spec:
  type: ClusterIP
  selector:
    app: cloudit-api
  ports:
    - name: http
      port: 8000
      targetPort: http
