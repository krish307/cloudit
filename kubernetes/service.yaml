apiVersion: v1
kind: Service
metadata:
  name: cloudit-service
  namespace: cloudit
spec:
  selector:
    app: cloudit
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort