apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  namespace: cloudit
  labels:
    app: postgres
    component: database
spec:
  clusterIP: None
  selector:
    app: postgres
    component: database
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP